---
- name: "🚀 LXC Container Deployment auf Proxmox"
  hosts: proxmox
  become: yes
  gather_facts: yes
  vars_files:
    - ../group_vars/all/vault.yml
  roles:
    - role: proxmox_lxc
      tags: [deployment, lxc]
